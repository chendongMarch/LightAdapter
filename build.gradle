apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion '25.0.3'
    defaultConfig {
        minSdkVersion 15
    }
}

dependencies {
    compile "com.android.support:recyclerview-v7:25.3.0"
}

File file = file('../bintrayConfig.gradle')
if(file.exists()){
    apply from: "../bintrayConfig.gradle"
}
