apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion '25.0.3'
    defaultConfig {
        minSdkVersion 15
    }
}

dependencies {
    compile rootProject.ext.google.appcompat_v7
    compile rootProject.ext.google.design
}
