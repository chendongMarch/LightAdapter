apply plugin: 'com.android.library'

android {
    compileSdkVersion 26
}

dependencies {
    api rootProject.ext.google.recyclerView_v7
}

File file = file('../bintrayConfig.gradle')
if(file.exists()){
    apply from: "../bintrayConfig.gradle"
}
